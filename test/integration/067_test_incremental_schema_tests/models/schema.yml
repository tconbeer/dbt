version: 2

models:
  - name: model_a
    columns:
      - name: id
        tags: [column_level_tag]
        tests:
          - unique

  - name: incremental
    columns:
      - name: id
        tags: [column_level_tag]
        tests:
          - unique
